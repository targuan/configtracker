apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: configtracker
  labels:
    app: configtracker
    component: frontend
spec:
  selector:
    app: configtracker
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
